{
  "policyVersion": "1.0.0",
  "baselineDate": "2026-01-01",
  "framework": "@storybook/nextjs-vite",
  "storybookVersion": "10.2.19",
  "requiredAddons": [
    "@storybook/addon-vitest",
    "@storybook/addon-a11y",
    "@storybook/addon-designs",
    "@chromatic-com/storybook"
  ],
  "requiredImports": {
    "test": "storybook/test",
    "previewApi": "storybook/preview-api",
    "actions": "storybook/actions"
  },
  "disallowedLegacyPackages": [
    "@storybook/addon-interactions",
    "@storybook/experimental-addon-test",
    "@storybook/test-runner",
    "@storybook/test"
  ],
  "configFiles": [".storybook/main.ts", ".storybook/preview.ts"],
  "validationCommands": [
    "node .ds-skills/project.mjs validate storybook-version-policy .storybook/storybook-version-policy.json",
    "node .ds-skills/project.mjs validate storybook-tier-policy storybook/component-tier-policy.json",
    "node .ds-skills/project.mjs validate storybook-story-inventory storybook/story-inventory.json"
  ],
  "notes": [
    "Keep all Storybook packages on the same exact release line.",
    "Review and intentionally update this file on every Storybook upgrade.",
    "validationCommands use the generated project launcher and shipped schema names, so they never depend on an internal installed path. They are shape checks. Where configured, this product also ships semantic storybook-policy plus ledger/proof commands; consumer-specific application validators are additional rather than replacements for those product contracts."
  ]
}
